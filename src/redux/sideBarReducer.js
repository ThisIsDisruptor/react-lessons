let friends = [
    { id: 1, name: 'Dimas' },
    { id: 2, name: 'Dryus' },
    { id: 4, name: 'Zaxar' },
];

let initialState = {
    friends,
};

const sideBarReducer = (state = initialState, action) => {

    return state;
}

export default sideBarReducer;